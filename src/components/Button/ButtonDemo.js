import Button from '.';

const ButtonDemo = () => {
  return (
    <div>
      <Button primary>primary</Button>
      <Button secondary>secondary</Button>
    </div>
  )
};

export default ButtonDemo;
